{
  "name": "paleneosort",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PaleNeØ song sorter, sort songs head-to-head until a final ranking emerges.",
  "scripts": {
    "build": "npm run format && npm run format:check && npm run test",
    "start": "http-server -p 8000 -o",
    "stop": "kill-port 8000",
    "set-accent": "node scripts/set-accent.mjs"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "kill-port": "^2.0.1"
  }
}
